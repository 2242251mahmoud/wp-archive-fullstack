# Set your deployed URLs for post-deploy smoke tests.
# The workflow will skip checks while these are placeholders.
SMOKE_FRONTEND_URL=https://your-frontend-domain
SMOKE_API_HEALTH_URL=https://your-api-domain/api/health
